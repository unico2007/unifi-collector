global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: unifi-collector
    static_configs:
      - targets: ["collector:8080"]

  # Prometheus scraping itself (optional, handy for debugging).
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
